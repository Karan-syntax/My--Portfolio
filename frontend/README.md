# Thiranex Frontend

This directory contains the React frontend for the portfolio website. It was built using React, Vite, and Framer Motion.

## Commands

To run the frontend individually:

```bash
npm run dev
```

To build for production:

```bash
npm run build
```
